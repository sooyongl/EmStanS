# Estimate modal ESS cut scores based on user-given cut scores.

Estimate modal ESS cut scores based on user-given cut scores.

## Usage

``` r
estCutScore_mode_manual(data, information, manual_cutscore)
```
